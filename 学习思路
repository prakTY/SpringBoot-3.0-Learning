“先搭骨架，再填肉，循序渐进” 
学习 SpringBoot 的核心是 “能独立开发一个可运行的业务接口”，而不是一开始就掌握所有组件。

新手阶段，你只需要聚焦 3 个核心：
SpringBoot 基础（项目搭建、配置文件、注解使用）；
SpringMVC（写接口、接收参数、返回 JSON）；
MySQL + MyBatis-Plus（操作数据库）。
其他组件（Redis、MQ、MongoDB、安全框架等）都是 “锦上添花”，等你能独立写一个完整的业务接口（比如 “用户注册 - 登录 - 查询信息”），再逐个学习。

用 “最小可行项目” 驱动学习，拒绝空泛的理论
以一个简单的业务场景为目标，边做边学。

